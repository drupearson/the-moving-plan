# Supabase (public - safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Anthropic (server-only - never expose this to the browser or commit it)
ANTHROPIC_API_KEY=
